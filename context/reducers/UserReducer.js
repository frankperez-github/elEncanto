export const UserReducer = (state, action) => {
    switch(action.type) {
        case "LOGIN": {
            return state
        }
        case "LOGOUT": {
            return {}
        }
        
    }
}